---

const { title, id } = Astro.props;
---
<button id={id}>
  <span>{ title }</span>
  <slot></slot>
</button>

<style>
  button{
    appearance: none;
    height: 40px;
    background-color: rgb(var(--highlight));
    border:none;
    border-radius: 6px;
    padding-block:10px;
    padding-inline:16px;
    font-size: 14px;
    font-weight: 400;
    color: #1D1E27;
  }
</style>